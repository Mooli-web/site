# اگر ریشه‌ی وب به‌جای public/ روی این پوشه تنظیم شده، همه‌چیز را به public/ هدایت کن
Options -Indexes
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteCond %{REQUEST_URI} !^/public/
  RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
